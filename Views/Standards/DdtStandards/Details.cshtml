@using ServiceManual.Services
@using ServiceManual.Helpers
@model DdtStandardDetailDto
@{
    ViewData["Title"] = Model?.Title ?? "Standard Details";
    ViewBag.IsStandardDetailPage = true;
    var compassStandardUrl = ViewBag.CompassStandardUrl as string ?? "";
    var errorMessage = ViewBag.ErrorMessage as string;
}

@if (Model != null)
{
    var hasPurpose = !string.IsNullOrEmpty(Model.Purpose);
    var hasHowToMeet = !string.IsNullOrEmpty(Model.HowToMeet);
    var hasGovernance = !string.IsNullOrEmpty(Model.Governance);
    var hasRelatedGuidance = !string.IsNullOrEmpty(Model.RelatedGuidance);
    var hasCategories = Model.Categories != null && Model.Categories.Any(c => !string.IsNullOrEmpty(c.Name));
    var hasContents = hasPurpose || hasHowToMeet || hasGovernance || hasRelatedGuidance || hasCategories;
    var relatedStandards = ViewBag.RelatedStandards as List<DdtStandardDto> ?? new List<DdtStandardDto>();
    var hasMeta = true; /* Part of: Standards always; optional: Last updated, Version */

<div class="guide-modern">
    @if (!string.IsNullOrEmpty(errorMessage))
    {
        <div class="govuk-grid-row">
            <div class="govuk-grid-column-full">
                <div class="govuk-error-summary" aria-labelledby="error-summary-title" role="alert" tabindex="-1" data-module="govuk-error-summary">
                    <h2 class="govuk-error-summary__title" id="error-summary-title">There is a problem</h2>
                    <div class="govuk-error-summary__body">
                        <ul class="govuk-list govuk-error-summary__list">
                            <li><a href="#main-content">@errorMessage</a></li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    }

    @* Hero: breadcrumb + header (same structure as DetailedGuide) *@
    <div class="govuk-grid-row">
        <div class="govuk-grid-column-full">
            <header class="guide-hero">
                <div class="govuk-width-container">
                    <nav class="govuk-breadcrumbs" aria-label="Breadcrumb">
                        <ol class="govuk-breadcrumbs__list">
                            <li class="govuk-breadcrumbs__list-item"><a class="govuk-breadcrumbs__link" href="/">Home</a></li>
                            <li class="govuk-breadcrumbs__list-item"><a class="govuk-breadcrumbs__link" href="@Url.Action("Index", "DdtStandards")">Standards</a></li>
                            <li class="govuk-breadcrumbs__list-item" aria-current="page">@Model.Title</li>
                        </ol>
                    </nav>
                    <div class="govuk-grid-row">
                        <div class="govuk-grid-column-two-thirds">
                            <span class="guide-hero__label">Standards</span>
                            <h1 class="govuk-heading-xl">@Model.Title</h1>
                            @if (!string.IsNullOrEmpty(Model.Summary))
                            {
                                <p class="govuk-body-l">@Model.Summary</p>
                            }
                        </div>
                    </div>
                </div>
            </header>

            @* Meta strip *@
            @if (hasMeta)
            {
                <div class="guide-meta">
                    <div class="govuk-width-container">
                        <span class="guide-meta__item"><strong>Part of:</strong> <a href="@Url.Action("Index", "DdtStandards")">Standards</a></span>
                        @if (Model.LastUpdated.HasValue)
                        {
                            <span class="guide-meta__dot" aria-hidden="true">·</span>
                            <span class="guide-meta__item"><strong>Last updated:</strong> @Model.LastUpdated.Value.ToString("dd MMMM yyyy")</span>
                        }
                        @if (!string.IsNullOrEmpty(Model.Version))
                        {
                            <span class="guide-meta__dot" aria-hidden="true">·</span>
                            <span class="guide-meta__item"><strong>Version:</strong> @Model.Version</span>
                        }
                    </div>
                </div>
            }

            @* Contents *@
            <div class="govuk-grid-row">
                <div class="govuk-grid-column-two-thirds">
                    <nav class="guide-contents guide-contents--dashed" aria-label="Contents">
                        <h2 class="guide-contents__title">Contents</h2>
                        <ul class="guide-contents__list">
                            @if (hasPurpose)
                            {
                                <li class="guide-contents__item">
                                    <span class="guide-contents__num" aria-hidden="true">—</span>
                                    <a href="#purpose">Purpose</a>
                                </li>
                            }
                            @if (hasCategories)
                            {
                                <li class="guide-contents__item">
                                    <span class="guide-contents__num" aria-hidden="true">—</span>
                                    <a href="#categories">Categories</a>
                                </li>
                            }
                            @if (hasHowToMeet)
                            {
                                <li class="guide-contents__item">
                                    <span class="guide-contents__num" aria-hidden="true">—</span>
                                    <a href="#how-to-meet">How to meet this standard</a>
                                </li>
                            }
                            @if (hasGovernance)
                            {
                                <li class="guide-contents__item">
                                    <span class="guide-contents__num" aria-hidden="true">—</span>
                                    <a href="#governance">Governance</a>
                                </li>
                            }
                            @if (hasRelatedGuidance)
                            {
                                <li class="guide-contents__item">
                                    <span class="guide-contents__num" aria-hidden="true">—</span>
                                    <a href="#related-guidance">Related guidance</a>
                                </li>
                            }
                            <li class="guide-contents__item">
                                <span class="guide-contents__num" aria-hidden="true">—</span>
                                <a href="#version-history">Version history</a>
                            </li>
                        </ul>
                    </nav>
                </div>
            </div>
        </div>
    </div>

    @* Content + sidebar *@
    <div class="govuk-grid-row guide-content-row">
        <div class="govuk-grid-column-two-thirds">
            <section class="dfe-f-content-section guide-content-section" aria-label="Content" id="standard-content">
                @if (Model.LegalStandard == true)
                {
                    <div class="govuk-warning-text">
                        <span class="govuk-warning-text__icon" aria-hidden="true">!</span>
                        <strong class="govuk-warning-text__text">
                            <span class="govuk-visually-hidden">Warning</span>
                            You are breaking the law if you do not meet this standard.
                        </strong>
                    </div>
                }

                @if (hasPurpose)
                {
                    <h2 class="govuk-heading-l" id="purpose">Purpose</h2>
                    <div class="dfe-f-body">@Html.Raw(GovUkMarkdownHelper.ToGovUkHtml(Model.Purpose))</div>
                }
                @if (Model.Categories != null && Model.Categories.Any())
                {
                    var categoriesGrouped = Model.Categories
                        .Where(c => !string.IsNullOrEmpty(c.Name))
                        .GroupBy(c => c.Name!, StringComparer.OrdinalIgnoreCase)
                        .OrderBy(g => g.Key);
                    <h2 class="govuk-heading-l govuk-!-margin-top-6" id="categories">Categories</h2>
                    <div class="govuk-!-margin-bottom-6">
                        @foreach (var group in categoriesGrouped)
                        {
                            var subCategories = group
                                .SelectMany(g => g.SubCategories ?? Enumerable.Empty<DdtStandardSubCategoryDto>())
                                .Where(sc => !string.IsNullOrEmpty(sc.Name))
                                .Select(sc => sc.Name!)
                                .Distinct(StringComparer.OrdinalIgnoreCase)
                                .OrderBy(n => n, StringComparer.OrdinalIgnoreCase)
                                .ToList();
                            <div class="govuk-!-margin-bottom-4">
                                <p class="govuk-body govuk-!-margin-bottom-1"><strong>@group.Key</strong></p>
                                @if (subCategories.Any())
                                {
                                    <ul class="govuk-list govuk-list--bullet govuk-body-s govuk-!-margin-left-2" style="color: #505a5f;">
                                        @foreach (var subName in subCategories)
                                        {
                                            <li>@subName</li>
                                        }
                                    </ul>
                                }
                            </div>
                        }
                    </div>
                }
                @if (hasHowToMeet)
                {
                    <h2 class="govuk-heading-l" id="how-to-meet">How to meet this standard</h2>
                    <div class="dfe-f-body">@Html.Raw(GovUkMarkdownHelper.ToGovUkHtml(Model.HowToMeet))</div>
                }
                @if (hasGovernance)
                {
                    <h2 class="govuk-heading-l" id="governance">Governance</h2>
                    <div class="dfe-f-body">@Html.Raw(GovUkMarkdownHelper.ToGovUkHtml(Model.Governance))</div>
                }
                @if (hasRelatedGuidance)
                {
                    <h2 class="govuk-heading-l" id="related-guidance">Related guidance</h2>
                    <div class="dfe-f-body">@Html.Raw(GovUkMarkdownHelper.ToGovUkHtml(Model.RelatedGuidance))</div>
                }
                <div class="govuk-inset-text govuk-!-margin-top-6">
                    <p class="govuk-body">
                        <strong>To view more details of this standard, view it in <a href="@compassStandardUrl" class="govuk-link" target="_blank" rel="noopener noreferrer">COMPASS</a>.</strong>
                    </p>
                </div>
            </section>
        </div>
        <div class="govuk-grid-column-one-third">
            @if (relatedStandards.Any())
            {
                <aside class="guide-sidebar__card" aria-label="Related standards">
                    <h3 class="govuk-heading-s">Related standards</h3>
                    <ul class="govuk-list govuk-list--spaced govuk-body-s">
                        @foreach (var s in relatedStandards)
                        {
                            <li class="govuk-!-margin-bottom-2">
                                <a href="@Url.Action("Details", "DdtStandards", new { slug = s.Slug })" class="govuk-link">@(s.Title ?? "Standard")</a>
                            </li>
                        }
                    </ul>
                </aside>
            }
        </div>
    </div>

    <div class="govuk-grid-row govuk-!-margin-top-8 govuk-!-padding-top-6" style="border-top: 1px solid #b1b4b6;">
        <div class="govuk-grid-column-full">
            <details class="govuk-details dfe-version-history" id="version-history" data-module="govuk-details">
                <summary class="govuk-details__summary">
                    <span class="govuk-details__summary-text">Version history</span>
                </summary>
                <div class="govuk-details__text">
                    <table class="govuk-table dfe-version-history__table">
                        <thead class="govuk-table__head">
                            <tr class="govuk-table__row">
                                <th scope="col" class="govuk-table__header">Version</th>
                                <th scope="col" class="govuk-table__header">Date</th>
                                <th scope="col" class="govuk-table__header">Details</th>
                            </tr>
                        </thead>
                        <tbody class="govuk-table__body">
                            <tr class="govuk-table__row">
                                <td class="govuk-table__cell">@(Model.Version ?? "–")</td>
                                <td class="govuk-table__cell">@(Model.LastUpdated?.ToString("dd MMMM yyyy") ?? Model.FirstPublished?.ToString("dd MMMM yyyy") ?? "–")</td>
                                <td class="govuk-table__cell">Current version</td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </details>
        </div>
    </div>
</div>
}
else
{
    <div class="govuk-width-container">
        <div class="govuk-grid-row">
            <div class="govuk-grid-column-two-thirds">
                <h1 class="govuk-heading-xl">Standard not found</h1>
                <p class="govuk-body">The standard you are looking for could not be found.</p>
                <p class="govuk-body"><a href="@Url.Action("Index", "DdtStandards")" class="govuk-link">Return to DDT standards list</a></p>
            </div>
        </div>
    </div>
}
