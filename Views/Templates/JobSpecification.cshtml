@using ServiceManual.Helpers
@using ServiceManual.Models
@{
    var jobSpec = ViewBag.JobSpecification as JobSpecification ?? new JobSpecification();
    var roleDescriptionHtml = GovUkMarkdownHelper.ToGovUkHtmlForBody(jobSpec.RoleDescription);
    var skillsHtml = GovUkMarkdownHelper.ToGovUkHtmlForBody(jobSpec.Skills);
    ViewData["Title"] = jobSpec.Title;
    var collectionSlug = ViewBag.CollectionSlug as string;
    var collectionTitle = ViewBag.CollectionTitle as string;
    var hasCollection = !string.IsNullOrEmpty(collectionSlug) && !string.IsNullOrEmpty(collectionTitle);
    var hasRightPanel = hasCollection || jobSpec.EnableWordDocDownload;
    var professionTitle = jobSpec.Profession?.Title ?? "Job descriptions";
}

@section Breadcrumbs {
<li class="govuk-breadcrumbs__list-item"><a class="govuk-breadcrumbs__link" href="/">Home</a></li>
<li class="govuk-breadcrumbs__list-item"><a class="govuk-breadcrumbs__link" href="/guidance">Guidance</a></li>
@if (!string.IsNullOrEmpty(professionTitle))
{
    <li class="govuk-breadcrumbs__list-item">@professionTitle</li>
}
<li class="govuk-breadcrumbs__list-item" aria-current="page">@jobSpec.Title</li>
}

@* Row 1: header + other roles nav (two-thirds only) *@
<div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">

        <div class="dfe-f-header">
            <span class="govuk-caption-xl govuk-!-margin-top-5">Job description</span>
            <h1 class="govuk-heading-xl dfe-f-header__title">@jobSpec.Title</h1>
            @if (!string.IsNullOrEmpty(jobSpec.Grade))
            {
                <p class="govuk-body-l govuk-!-margin-top-2 govuk-!-margin-bottom-0">Grade @jobSpec.Grade</p>
            }
        </div>

        @if (jobSpec.SiblingJobSpecifications.Count > 0)
        {
            <nav class="dfe-f-guide-contents govuk-!-margin-top-6" aria-label="Other roles in this profession">
                <h2 class="dfe-f-guide-contents__title">Other roles in this profession</h2>
                <ul class="dfe-f-guide-contents__list">
                    @foreach (var sibling in jobSpec.SiblingJobSpecifications)
                    {
                        var isCurrent = sibling.Slug == jobSpec.Slug;
                        <li class="dfe-f-guide-contents__item dfe-f-guide-contents__item--dashed @(isCurrent ? "dfe-f-guide-contents__item--active" : "")">
                            <span class="dfe-f-guide-contents__dash" aria-hidden="true"></span>
                            @if (isCurrent)
                            {
                                <span aria-current="page">@sibling.Title</span>
                            }
                            else
                            {
                                var siblingHref = "/guidance/job-specifications/" + sibling.Slug + (hasCollection ? "?collection=" + Uri.EscapeDataString(collectionSlug!) : "");
                                <a class="govuk-link dfe-f-guide-contents__link" href="@siblingHref">@sibling.Title</a>
                            }
                        </li>
                    }
                </ul>
            </nav>
        }

    </div>
</div>

@* Row 2: body content + right panel *@
<div class="govuk-grid-row govuk-!-margin-top-6">
    <div class="@(hasRightPanel ? "govuk-grid-column-two-thirds" : "govuk-grid-column-full")">

        @if (!string.IsNullOrWhiteSpace(jobSpec.RoleDescription))
        {
            <section class="govuk-!-margin-bottom-8" aria-labelledby="you-will-heading">
                <h2 id="you-will-heading" class="govuk-heading-l">You will:</h2>
                <div class="dfe-f-body">
                    @Html.Raw(roleDescriptionHtml)
                </div>
            </section>
        }

        @if (!string.IsNullOrWhiteSpace(jobSpec.Skills))
        {
            <section class="govuk-!-margin-bottom-8" aria-labelledby="skills-heading">
                <h2 id="skills-heading" class="govuk-heading-l">Skills you need</h2>
                <div class="dfe-f-body">
                    @Html.Raw(skillsHtml)
                </div>
            </section>
        }

    </div>

    @if (hasRightPanel)
    {
        <div class="govuk-grid-column-one-third">
            @if (hasCollection)
            {
                <aside class="dfe-f-sidebar-box dfe-f-sidebar-box--highlight" aria-label="Part of">
                    <div class="dfe-f-sidebar-box__section">
                        <h3>Part of</h3>
                        <ul class="govuk-list govuk-list--spaced">
                            <li><a class="govuk-link" href="/guidance/collections/@collectionSlug"><strong>@collectionTitle</strong></a></li>
                        </ul>
                    </div>
                </aside>
            }
            @if (jobSpec.EnableWordDocDownload)
            {
                <aside class="dfe-f-sidebar-box" aria-label="Download">
                    <div class="dfe-f-sidebar-box__section">
                        <h3>Download</h3>
                        <p class="govuk-body-s govuk-!-margin-bottom-0">
                            <a href="/guidance/job-specifications/@jobSpec.Slug/download" class="govuk-link" download>
                                Word document
                            </a>
                        </p>
                    </div>
                </aside>
            }
        </div>
    }
</div>
